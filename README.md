# saens

MIDI のノート列に合わせて、入力オーディオをピッチ近傍で切り貼りする実験的な TypeScript ライブラリです。

## インストール

```bash
npm i @kongyo2/saens
```

## npm 公開（手動）

GitHub Actions の **Publish to npm** を手動実行します。  
実行前にリポジトリの Secrets に `NPM_TOKEN` を設定してください。
